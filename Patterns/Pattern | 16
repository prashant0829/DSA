void alphaRamp(int n)
{
    int a = 65;
    for (int i = 0; i < n; i++)
    {
        for (int j = 0; j <= i; j++)
        {
            std::cout << static_cast<char>(i + a) << " ";
        }
        std::cout << endl;
    }
}
